To do:

        - Create a user guide
        - Add multiple tags per object, split by comma
        - Sort by tag
        - Checkbox to auto save file on edit, else use a save button
        - Watch the storage folder for any changes, and auto refresh
        - Export as .txt
            - Choose directory to export
        - Auto sort tag for new file?
        - Add trashcan that deletes files that have been there for more than 30 days

        - Scrollbar position resets on refresh

        - change json parsing to 2 files (one for sorted, one for unsorted) for faster performance.

        - Application Icon:
            - Apply Icon to all popout windows (cloud window, settings window)
            - Invert Icon color based on light/dark mode


Settings to modify
    - Light/Dark Mode (boolean)
    - Option to push data on close (boolean)
    - Option to pull data on open (boolean)

    - KeyBinds
    - change date/time format



Current Keybinds:
    - CTRL + Z = Undo
    - CTRL + Y = Redo
    - CTRL + N = New File
    - CTRL + D = Delete current file
    - CTRL + Q = Sort file
    - CTRL + P = Push to database
    - CTRL + SHIFT + P = Pull from database
    - F5 = Refresh

